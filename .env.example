# Cloud agent API tokens — copy to .env and fill in real values
# .env is gitignored; never commit actual tokens

# OpenClaw cloud agent
OPENCLAW_CLOUD_TOKEN=your-token-here

# Custom self-hosted agent (if token_env matches in cloud-agents.json)
# MY_AGENT_TOKEN=your-token-here
